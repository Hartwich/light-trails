# AI Agent Guide

- This repo contains only the Light Trails game package.
- Do not make it standalone unless the platform contract changes.
- Keep public exports limited to manifest, protocol, server, host, and controller.
- Test through the Open Party Lab platform with `npm run games:sync-local`.
